import * as actions from "./actions"
import * as selectors from "./selectors"
import reducers from "./reducers"
import * as types from "./actionTypes";

export default { reducers, actions, types, selectors };
